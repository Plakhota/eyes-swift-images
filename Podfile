# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'HelloWorldiOS' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HelloWorldiOS

end

target 'EyesImagesTests(UnitTests)' do
  pod 'EyesImages'
end

target 'EyesXCUITests(UITests)' do
    pod 'EyesXCUI'
end
